# BACKLOG

> **Последнее обновление**: 2026-01-17  
> **Статус проверен**: Проанализированы текущие реализации в кодовой базе

---

## High Priority 🔴

### 1. **Data Match**: Аудит расхождения структур Legacy vs New Clients
- **Статус**: ❌ НЕ НАЧАТО
- **Зачем**: В проекте могут существовать две версии `clients`: старая legacy структура и новая. Расхождение в колонках или типах данных может привести к потере данных при миграции или дублированию записей.
- **Проблема**: Неясно, существует ли вообще legacy `core.clients` отдельно от текущего `core.clients`, или это устаревшая задача.
- **Что нужно**:
  1. Запустить `scripts/check_tables.py` и проверить наличие duplicate таблиц.
  2. Если найдены — выполнить SQL diff (сравнить колонки, типы, constraints).
  3. Создать миграцию для слияния или обозначить одну как устаревшую.
- **Критичность**: СРЕДНЯЯ (если legacy таблица не используется), ВЫСОКАЯ (если идет параллельное использование).

---

### 2. **Validation Fix**: Исправить ошибки валидации в `expenses_hst` и `clients_cur`
- **Статус**: ❌ НЕ НАЧАТО (contracts для `expenses` не найдены)
- **Зачем**: Если контракт (схема данных) не соответствует реальным заголовкам в Google Sheets, то валидатор будет отбрасывать строки как "неизвестные колонки" или "отсутствующие поля". Это приводит к потере данных при загрузке.
- **Текущая ситуация**:
  - В `sources.yml` найдена ссылка на `expenses_hst` (строки 120-130), но **контракты для этой таблицы отсутствуют** в проекте.
  - Для `clients_cur` контракт существует, но нужна проверка соответствия заголовков Sheets.
- **Что нужно**:
  1. Выполнить `python scripts/inspect_sheets.py` — получить актуальные заголовки из Sheets.
  2. Сравнить с контрактами в `contracts/` (создать `expenses.yml` если отсутствует).
  3. Обновить контракты или маппинг в `sources.yml`.
  4. Запустить ETL с `--dry-run` и проверить validation_logs на ошибки.
- **Критичность**: ВЫСОКАЯ (потеря данных при каждом запуске).

---

### 3. **Ops**: Автоматическая ротация/очистка `validation_logs`
- **Статус**: ❌ НЕ НАЧАТО
- **Зачем**: Таблица `ops.validation_logs` накапливает записи об ошибках валидации при каждом запуске ETL. Без очистки она может вырасти до сотен тысяч строк, замедляя запросы и занимая место в БД.
- **Текущая ситуация**: Существует вручную запускаемый `scripts/clean_logs.py`, который удаляет логи за сегодня. Автоматизации нет.
- **Что нужно**:
  1. Добавить в `src/etl/pipeline.py` метод `_cleanup_old_logs()` с логикой удаления записей старше N дней (например, 30).
  2. Вызывать его в начале каждого запуска пайплайна (`ELTPipeline.run()`).
  3. Альтернатива: настроить Supabase TTL policy на уровне БД (через SQL миграцию).
  4. Добавить параметр в `settings.py`: `LOG_RETENTION_DAYS = 30`.
- **Критичность**: СРЕДНЯЯ (проблема накапливается постепенно, но неизбежна).

---

### 4. **Business Logic**: Переработка витрины `v_client_balances`
- **Статус**: ⚠️ ЧАСТИЧНО РЕАЛИЗОВАНО (использует хрупкую логику regex)
- **Зачем**: Текущая логика расчета купленных занятий **парсит название продукта регулярками** (строки 10-14 в `view_client_balances.sql`):
  ```sql
  WHEN product_name ~ '8' THEN 8
  WHEN product_name ~ '4' THEN 4
  ```
  Это **ненадежно**: если продукт назван "Абонемент на 4 занятия + подарок", регулярка `'4'` сработает, но если название изменится на "Четыре занятия" — сломается.
- **Что нужно**:
  1. Использовать колонку `quantity` из `core.sales` напрямую (уже есть в таблице).
  2. Если нужна связь продукта и кол-ва занятий — создать lookup таблицу `lookups.products` (product_name → units_per_item).
  3. Переписать view:
     ```sql
     SUM(s.quantity * COALESCE(p.units_per_item, 1)) as units_bought
     FROM core.sales s
     LEFT JOIN lookups.products p ON s.product_name = p.name
     ```
- **Критичность**: ВЫСОКАЯ (бизнес-метрика, некорректные остатки → финансовые проблемы).

---

### 5. **Smart Header Mapping**: Автоматическое предложение маппинга при опечатках
- **Статус**: ❌ НЕ НАЧАТО
- **Зачем**: Если пользователь переименовал колонку в Sheets с "Клиент" на "Клиенты" (добавил букву), текущая система **молча игнорирует** эту колонку. Умный маппер мог бы предложить: "Не найдена колонка 'Клиент', но есть похожая 'Клиенты'. Использовать?"
- **Что нужно**:
  1. Установить библиотеку `python-Levenshtein` (или использовать встроенный `difflib`).
  2. В `src/etl/extractor.py` добавить метод `_suggest_mapping()`.
  3. При несовпадении заголовка вычислять расстояние Левенштейна до всех существующих колонок.
  4. Если расстояние < 3 и similarity > 0.8 — логировать предупреждение: "Возможно, 'Klent' = 'klient'?"
  5. (Опционально) Добавить интерактивный режим подтверждения.
- **Критичность**: СРЕДНЯЯ (качество жизни, но может предотвратить критическую потерю данных).

---

### 6. **Telegram Alerts**: Оповещение при критической остановке пайплайна
- **Статус**: ❌ НЕ НАЧАТО
- **Зачем**: Если ETL падает (например, Google API quota exceeded или ошибка БД), **никто не узнает**, пока не обнаружат устаревшие данные в dashboard. Telegram-алерты позволяют немедленно реагировать.
- **Что нужно**:
  1. Создать `src/utils/telegram_notifier.py` с методом `send_alert(message)`.
  2. Использовать `python-telegram-bot` или `requests` для отправки через Telegram Bot API.
  3. Добавить в `.env`:
     ```
     TELEGRAM_BOT_TOKEN=***
     TELEGRAM_ALERT_CHAT_ID=***
     ```
  4. В `src/main.py` обернуть весь pipeline в try/except:
     ```python
     except Exception as e:
         telegram_notifier.send_alert(f"🚨 ETL PIPELINE FAILED:\n{e}")
         raise
     ```
- **Критичность**: ВЫСОКАЯ (observability — критично для production).

---

### 7. **Data Merging**: Доработка SQL скриптов для объединения `_cur` и `_hst`
- **Статус**: ✅ **УЖЕ РЕАЛИЗОВАНО**
- **Факт**: Скрипты `transform_sales.sql` и `transform_schedule.sql` **уже содержат** два отдельных `MERGE` запроса (один для `_cur`, второй для `_hst`). См. строки 6-70 и 73-140 в `transform_sales.sql`.
- **Что можно улучшить** (опционально):
  - Объединить два MERGE в один с помощью `UNION ALL` в подзапросе.
  - Добавить явное управление `source` полем (сейчас оно перезаписывается последним источником).
- **Действие**: Пометить как DONE или переформулировать задачу на "Оптимизация объединения".

---

## Medium Priority 🟡

### **Advanced Validation Rules**: Логическая связность данных
- **Статус**: ❌ НЕ НАЧАТО
- **Зачем**: Предотвратить "глупые" ошибки ввода: дата начала тренировки позже даты окончания, отрицательная цена, клиент моложе 0 лет.
- **Что нужно**: Расширить `src/etl/validator.py`, добавить `BusinessRuleValidator` с проверками cross-field логики.

### **Performance Tracking**: Графики времени выполнения в Dashboard
- **Статус**: ❌ НЕ НАЧАТО
- **Зачем**: Понимать узкие места (что медленнее: Extract, Transform, Load?).
- **Что нужно**: Логировать время каждого этапа в `ops.elt_runs`, визуализировать в `dashboard.py`.

---

## Low Priority 🟢

### **Dockerization**: Изоляция окружения
- **Статус**: ❌ НЕ НАЧАТО
- **Зачем**: Упрощает разворачивание на новых серверах, CI/CD.
- **Что нужно**: `Dockerfile` + `docker-compose.yml`.

### **API Access**: FastAPI слой над данными
- **Статус**: ❌ НЕ НАЧАТО
- **Зачем**: Доступ к данным через REST API вместо прямых SQL запросов.
- **Что нужно**: `src/api/` с FastAPI endpoints.

---

## Future: Hybrid Strategy (Sheets + Web App) 🔵
*Status: Deferred / Концептуальная проработка*

[Содержимое без изменений]
