# 🧭 Индекс Скриптов / Scripts Navigation Index

> **Версия**: 1.0  
> **Дата обновления**: 2026-01-17  
> **Назначение**: Быстрая навигация по утилитам проекта для разработчиков и AI-агентов.

Этот документ описывает назначение всех Python-скриптов в проекте, разделяя их по категориям использования.

---

## 🚀 Основные Точки Входа (Entry Points)

Скрипты для выполнения основных задач бизнес-логики.

| Скрипт | Описание | Пример запуска |
| :--- | :--- | :--- |
| [`src/main.py`](../src/main.py) | **Главный ETL Пайплайн**. Запускает полный цикл: Выгрузка → Трансформация → Экспорт. Поддерживает режимы CDC и Full Refresh. | `python src/main.py` <br> `python src/main.py --full-refresh` |
| [`dashboard.py`](../dashboard.py) | **Streamlit Дашборд**. Веб-интерфейс для визуализации бизнес-метрик и статуса системы. | `streamlit run dashboard.py` |

---

## 🏥 Диагностика и Здоровье (Health & Diagnostics)

Скрипты "ПЕРВОЙ ПОМОЩИ" для проверки работоспособности системы.

| Скрипт | Описание | AI Hint (Когда использовать) |
| :--- | :--- | :--- |
| [`scripts/health_check.py`](../scripts/health_check.py) | **Комплексная диагностика**. Проверяет:<br>1. `.env` переменные<br>2. Google Auth<br>3. Коннект к БД<br>4. Структуру схем<br>5. Примеры данных | *Если "ничего не работает" или упало соединение.* |
| [`scripts/inspect_sheets.py`](../scripts/inspect_sheets.py) | **Инспектор Google Sheets**. Выгружает заголовки из всех настроенных таблиц в `headers.json`. Помогает выявить изменения структуры данных на стороне источника. | *Если ETL падает с ошибкой маппинга или "column not found".* |
| [`scripts/check_tables.py`](../scripts/check_tables.py) | **Листинг Таблиц**. Выводит список всех таблиц во всех схемах БД. | *Для быстрой проверки наличия таблиц после миграций.* |
| [`scripts/whoami.py`](../scripts/whoami.py) | **Check User**. Проверяет, под каким пользователем БД скрипт подключен. | *При проблемах с правами доступа (Permission Denied).* |

---

## 🧹 Обслуживание и Очистка (Maintenance)

Скрипты для регулярных регламентных работ.

| Скрипт | Описание | Пример запуска |
| :--- | :--- | :--- |
| [`scripts/clean_logs.py`](../scripts/clean_logs.py) | **Очистка Логов**. Удаляет записи из `validation_logs` за сегодня. | `python scripts/clean_logs.py` |
| [`scripts/clean_legacy_logs.py`](../scripts/clean_legacy_logs.py) | **Cleaner**. Удаляет "битые" записи из статистических таблиц в `ops`. | `python scripts/clean_legacy_logs.py` |

---

## 🐛 Глубокая Отладка (Deep Debugging)

Специализированные скрипты для решения узких проблем.

| Скрипт | Описание |
| :--- | :--- |
| [`scripts/diagnostic_duplicates.py`](../scripts/diagnostic_duplicates.py) | **Поиск дубликатов**. Ищет записи с одинаковым `__row_hash` в staging таблицах. Полезен для отладки CDC. |
| [`scripts/verify_core_columns.py`](../scripts/verify_core_columns.py) | **Проверка Колонок**. Выводит примеры данных из критически важных колонок (admin, trainer, employee_id), чтобы убедиться, что они не NULL. |
| [`scripts/debug_dashboard_data.py`](../scripts/debug_dashboard_data.py) | **Debug Dashboard**. Скрипт выводит в консоль те же цифры, что должны быть на дашборде, для сверки backend vs frontend. |

---

## 🧪 Тестирование (Stress & Chaos)

| Скрипт | Описание |
| :--- | :--- |
| [`scripts/chaos_runner.py`](../scripts/chaos_runner.py) | **Chaos Monkey**. Запускает ETL в бесконечном цикле с случайными задержками и помехами. Строго для тестовых окружений! |

---

## 📂 Прочее

*   `alembic/` - Управление миграциями БД (`alembic upgrade head`).
*   `headers.json` - Кеш заголовков, генерируемый `inspect_sheets.py`.
*   `.env` - Конфигурация окружения (пароли, ключи).
*   `sources.yml` - Конфигурация источников (Google Sheets ID).
